language: node_js
sudo: required
node_js:
  - "8"
branches:
  only:
    - master
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - npm install -g @angular/cli
cache:
  directories:
    - "node_modules"
